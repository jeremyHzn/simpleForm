{{ form_start(questionForm) }}
    {{ form_row(questionForm.email) }}
    {{ form_row(questionForm.question1) }}
    {{ form_row(questionForm.question2) }}
    {{ form_row(questionForm.question3) }}
    <button type="submit" class="mt-2 btn btn-success">Envoyer</button>
{{ form_end(questionForm) }}


